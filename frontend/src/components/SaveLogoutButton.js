import React from 'react';

import '../styles/SaveLogoutButton.css';

function SaveLogoutButton() {
    return (

        <div>
            <button type='button' id='SaveLogoutButton' className='btn-custom w-100 overflow-hidden'>Logout</button>
        </div>

    );
};

export default SaveLogoutButton;